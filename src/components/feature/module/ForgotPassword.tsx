'use client'
import React from 'react'

interface Props {
  onBack: () => void;
}
const ForgotPassword = ({
  onBack,
}: Props) => {
  return (
    <div>
      ForgotPassword
    </div>
  )
}

export default ForgotPassword
